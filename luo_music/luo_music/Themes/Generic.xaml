<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local1="using:luo_music.Common">


    <Style TargetType="local1:TopNavigationControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local1:TopNavigationControl">
                    <StackPanel Orientation="Vertical">
                        <StackPanel.Resources>
                            <local1:ColorToBrushConverter x:Key="Converter" />
                        </StackPanel.Resources>
                        <StackPanel x:Name="RootPanel" Orientation="Horizontal" />
                        <Canvas>
                            <Border
                                x:Name="NavigationBorder"
                                Width="25"
                                Height="4"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Background="{Binding Path=SliderBrush, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay, Converter={StaticResource Converter}}" />
                        </Canvas>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>
